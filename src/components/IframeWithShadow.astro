---
const { src, title = "Hydra Sketch" } = Astro.props;
---

<div class="relative w-full max-w-5xl mx-auto shadow-2xl/50 shadow-black rounded-2xl overflow-hidden">
  <div class="relative w-full aspect-[16/9]">
    <iframe
      class="absolute top-0 left-0 w-full h-full border-none opacity-0 transition-opacity duration-1000 ease-in"
      src={src}
      title={title}
      allowfullscreen
      onload="this.classList.remove('opacity-0')"
    ></iframe>
  </div>
</div>
